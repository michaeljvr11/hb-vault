# Session 30 — TE-1/TE-2/TE-3: Transactional email foundations

**Date:** 2026-08-15 · **Cards:** [TE-1](https://trello.com/c/e7WlyfLC) / [TE-2](https://trello.com/c/vFpGIEUF) / [TE-3](https://trello.com/c/0DwUaRkX) · **Branch:** `feat/e7WlyfLC-transactional-email-foundations` · **Status:** PR ready

- Shipped: Branded HTML email template (`email-template.ts` with `renderEmail()`, `EmailContentBlock[]` API, multi-recipient `send(to: string | string[])`). Platform admin settings: `platform_settings` table + `/api/admin/settings` GET/PATCH + audit logging. Vendor email override: `vendors.notificationEmail` with fallback to account email. Both web pages (admin settings list editor, vendor profile field). Hardened `MailService.send()` with try/catch on render + client resolution + dispatch so transport failures never escape.
- Decisions: Render happens inside try/catch, not before `send()` — enforces the all-or-nothing safety guarantee. Multi-recipient on all three layers (platform settings array, vendor override, send signature) — one paid order can produce N vendor + 1 platform email. JSONB for `notificationEmails` column to match `vendors.profileSections` precedent. Format validation only on vendor email (no verify round trip) — vendor-owned operational config, not auth identity. Placeholder logo in one constant (`EMAIL_LOGO_MARKUP` text wordmark) for easy swap. Questions 3, 4, 5, 6, 7 resolved; Q1 and Q2 remain open (customer confirmation email, retry/queue policy).
- Tests: API 670/670 ✓, web 818/818 ✓, both migrations run → revert → re-run cleanly.
- Follow-ups: TE-4 needs a public method on `MailService` (e.g. `sendOrderPaidVendor()`) or visibility widened so domain-event listener can dispatch. Code review flagged: render/client calls were sitting outside try/catch; fixed in `2aa2348`.
