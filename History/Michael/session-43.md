# Session 43 — SEC-1..SEC-4: Security guards batch

**Date:** 2026-09-11 · **Card:** BGykPFS7, Jw33F6CV, vKRja5fR, NSSA9AXv · **Branch:** `feat/BGykPFS7-security-guards-batch` · **Status:** open

- Shipped: Static security headers on the HTML origin at the Caddy edge (SEC-1); env-driven basic-auth + `X-Robots-Tag: noindex` gate for non-production (SEC-2); gitleaks blocking + Dependabot + reporting-only `npm audit` in CI (SEC-3); CORS fails closed in production, with `https://hnb.co.za` — not a domain we own — removed from the fallback list (SEC-4). The 2026-07-02 audit's "security headers added / CSP added as interim" lines overstated scope for three months: helmet only ever covered `/api/*`, so the SSR app had no CSP, no `X-Frame-Options` and no `Permissions-Policy`. Corrected in [[Auth & Roles]].
- Decisions: The SEC-2 gate must fail OPEN — CI cannot write `/opt/hb/.env` by design. Code review found four reproducible ways it could take the whole site down, across two passes. Two were subtle: `docker compose` interpolates env-file values, so an unquoted bcrypt hash is silently mangled and survives roughly one time in five depending on the salt; and Caddy's `{$VAR:default}` uses set-semantics, so an empty value defeats the default. `basic_auth` also parses its hash at config *load* time regardless of whether its matcher fires, so a malformed hash kills the edge either way — `infra/deploy.sh` now refuses to deploy rather than let it crash-loop. SEC-1's headers are scoped to the web handle because the site-wide `defer` block replaces rather than appends, and would overwrite helmet on `/api/*`.
- Tests: API 1053/1053, lint clean, full build clean, gitleaks clean over full history, 33/33 behavioural checks against real Caddy through real `docker compose` env resolution.
- Follow-ups: SEC-5 (nonce CSP, card YYjbfc65) split out and still open; `localStorage` access-token refactor still open; confirm `CORS_ORIGINS` is present in `/opt/hb/.env` before merge (enforced by `deploy.sh`, but a missing value blocks the deploy).
