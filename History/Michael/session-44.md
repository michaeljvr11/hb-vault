# Session 44 — SEC-5, SEC-6, SEC-7, SEC-8: Security cleanup batch

**Date:** 2026-09-14 · **Cards:** SEC-5 `YYjbfc65`, SEC-6 `jU5VVkDp`, SEC-7 `XodVNFmi`, SEC-8 `z2NOBA3t` · **Branch:** `feat/YYjbfc65-sec-cleanup-batch` · **Status:** open

- Shipped: SEC-5 nonce-based CSP for HTML origin (per-request nonce, thread into Angular SSR render, patch unnonced inline tags, remove Caddyfile duplicate policy) + SEC-7 access token from localStorage to in-memory (via `AuthService`, session recovery on page load via `/auth/refresh` + httpOnly cookie) bundled to close audit M4 completely. SEC-6 Angular 21.2.23/24 patch bump + static guardrail against `HttpParams.append` (zero call sites today, confirmed; protects future refactors). SEC-8 Meilisearch scoped clients (`MEILI_SEARCH_KEY` read-only for queries, `MEILI_ADMIN_KEY` for writes; running API never reads `MEILI_MASTER_KEY`).
- Decisions: SEC-5 and SEC-7 bundled because SEC-5 restores interim control, SEC-7 is the real fix removing the need for one — lands M4 completely rather than stalling on either halfway. SEC-6/SEC-8 bundled as unrelated hardening from same pre-production review, owner-approved bundle exception per CLAUDE.md. SEC-8 prod env explicitly lists allowed keys to prevent master-key leak via env_file blanket. SEC-5 review caught three bugs (response truncation on prerender, stale ETags on 304, style-attr bindings blocked); SEC-7 review caught missed Concurrency requirement (single-slot token rotation logging out concurrent tabs — fixed with Web Locks choke point) + follow-up interceptor bugs (401 on login itself swallowed, failed refresh not clearing state — both fixed).
- Tests: API 78 suites / 1061 tests, Web 89 files / 1326 tests. Lint clean, full build clean.
- Follow-ups: None — all 4 cards complete and PR ready.
