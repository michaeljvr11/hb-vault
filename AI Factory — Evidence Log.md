---
type: process
tags:
  - ai-factory
  - evidence
  - hackathon
---

# AI Factory — Evidence Log
# AI Factory — Evidence Log
_Source: `npm run evidence`. Refresh each cycle; the repo `REPORT.md` is the source of truth._

**Snapshot (refreshed 2026-08-18)**

- **241** commits (2026-06-11 → 2026-08-18); **230** carry the AI-authorship trailer (**95%**)
- **+96,283 / −10,273** lines across **763** files
- **131** test specs compiled from the repo (API 61 · Web 70)
- Guardrails fired: **23** prod-fence blocks logged (push-to-protected, force-push, `gh pr merge`, merge/rebase on protected — all human-only) · **35** green PR gates · **247** lint issues fed back to agents
- Traceability (latest): `8AQq2C3E` PIO-1/`1f1W44bw` PIO-2/`0HQBCxik` PIO-3/`wLFJ22ps` PIO-4/`EgynexWb` PIO-5 product-image-optimization (branch `feat/8AQq2C3E-product-image-optimization`, [PR #59](https://github.com/michaeljvr11/hb-mono-repo/pull/59) open) — first five-card bundle shipped via `/ship-batch`; sequential slices, one commit each. PIO-1 dimension probe + `width`/`height`/`sizeBytes` contract fields (found and fixed a missing `fallbackToMimetype` that would have 422'd every real product upload); PIO-2 reusable `ImageProcessorService` + WebP derivatives + `memoryStorage` so the raw upload never reaches disk; PIO-3 one shared SSR-safe `srcset` helper for card + PDP; PIO-4 docs-only design slice (vendor presets measured from the stylesheet, dropping the proposed 960/1536 banner tiers; revised the proposed eight flat `VendorDto` fields to nested objects over a shared `ImageVariantDto` because the proposal duplicated an interface PIO-2 had already shipped); PIO-5 vendor branding through the same processor unmodified. Five open questions were put to the developer at the CLARIFY gate rather than assumed (WebP-only, caps, storage mode). Review caught three blockers: cleanup compensation covered only image processing, so an invalid `categoryId` orphaned 24 files and a failure mid-attach left a committed product with no images or audit log; the vendor path had no compensation at all; and the test named for cleanup asserted a call count and would have passed with the `catch` deleted. Known gap recorded honestly: the magic-number validation path is unexercised under ts-jest (ESM `file-type`), follow-up filed. Earlier: `Ah6EZCOW` LSM-4/`9y69dIul` LSM-5/`TkbgWKL2` LSM-6 contact-page-and-inquiries (branch `feat/Ah6EZCOW-contact-page-and-inquiries`, PR open) — LSM-5 `POST /inquiries` endpoint (public, throttled, persist-first with guarded Resend notify), ContactInquiry entity + native Postgres enum + migration, DTO; LSM-4 `/contact` page (reactive form, ContactService, native HTML `<select>`/`<checkbox>`, WhatsApp CTA, prerendered); LSM-6 footer/nav wiring (Contact Support, About, Services, Register as Vendor links live), Home active-state bug fix, brand unified to "H&B E-Commerce" across nav/footer/auth/shells. Validation parity failures caught in review (API DTO caps vs form, Validators.email vs class-validator, @IsNotEmpty needed); WhatsApp green contrast regressed (white text 1.98:1, dark label 8.6:1 fix applied). Earlier: `kdro0zYC` LSM-1/`xhYmG5j8` LSM-2/`ahPpaEIK` LSM-3 landing-site migration (assets + SITE_IMAGES constants; tokens swapped `--hb-primary` `#015300`→`#2e7d32`, `--hb-secondary` `#964900`→`#f57c00`; contrast regressed, 2-round fix; `/about` + `/services` prerendered, teaser deleted + `/shop` cross-link, RenderMode.Prerender resolves SSR spec Q2). Earlier: `wygImWJb` TE-4 & `dwkfUoZ0` TE-5 order-paid notifications (domain event `order.paid` fans out N vendor + 1 platform + 1 customer emails; best-effort handlers; money formatting fixed). Earlier: `e7WlyfLC` TE-1/TE-2/TE-3 transactional email foundations (PR #53 merged). Earlier: `EDR-1` Earnings date-range picker (PR #52 merged). See repo `REPORT.md` for complete per-card/per-PR history.
